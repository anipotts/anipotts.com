---
import { Icon } from "astro-icon/components";
import { formatDate, writingSlug, type Writing } from "../lib/content";
import AmbientFlow from "./AmbientFlow.astro";

interface Props {
  item: Writing;
  flowVariant?: number;
}

const { item, flowVariant } = Astro.props;
const slug = writingSlug(item);
const { title, summary, published_at } = item.data;
---

<a
  href={`/writing/${slug}`}
  class="card writing-card"
  data-astro-prefetch="viewport"
>
  <AmbientFlow kind="writing" seed={slug} variant={flowVariant} />
  <span class="head">
    <span class="title-group">
      <span class="title">{title}</span>
      {
        published_at && (
          <time class="date mono" datetime={published_at.toISOString()}>
            {formatDate(published_at)}
          </time>
        )
      }
    </span>
    <Icon name="ph:arrow-bend-right-up" class="affordance" />
  </span>
  <span class="sub">{summary}</span>
</a>

<style is:global>
  .writing-card {
    position: relative;
    isolation: isolate;
    contain: paint;
    overflow: hidden;
    display: flex;
    flex-direction: column;
    gap: var(--s-1);
    border-radius: var(--paper-radius);
    background: var(--paper-surface);
    color: var(--paper-ink);
    box-shadow: var(--paper-shadow);
    text-decoration: none;
    transition: background 0.2s;
  }

  .writing-card:hover {
    background: var(--paper-surface-hover);
  }

  .writing-card .head {
    position: relative;
    z-index: 1;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: var(--s-3);
  }

  .writing-card .title-group {
    display: flex;
    align-items: baseline;
    flex-wrap: wrap;
    gap: var(--s-1) var(--s-3);
    min-width: 0;
  }

  .writing-card .title {
    font-family: var(--display);
    font-size: var(--p-h2);
    font-weight: 900;
    line-height: 1;
    letter-spacing: 0;
    font-synthesis: none;
  }

  .writing-card .affordance {
    color: var(--paper-muted);
    flex-shrink: 0;
    transition:
      color 0.2s,
      transform 0.2s;
  }

  .writing-card:hover .affordance,
  .writing-card:focus-visible .affordance {
    color: var(--paper-accent);
  }

  .writing-card .sub {
    position: relative;
    z-index: 1;
    color: var(--paper-muted);
    font-size: var(--p-small);
  }

  .writing-card > .ambient-flow {
    inset: 0;
    height: 100%;
    border-radius: inherit;
  }

  .writing-card .date {
    color: var(--paper-muted);
    font-size: 0.8rem;
  }
</style>
