---
import { legacyEditRedirect } from "../../../lib/editorial-collections";
// Retired: the D1 content diagnostics. An old page key lands on its record.
return Astro.redirect(legacyEditRedirect(Astro.params.pageKey), 308);
---
