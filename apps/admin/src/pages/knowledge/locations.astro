---
import { dataRecordsHref } from "../../lib/data-routes";
// Retired: known places are Data records of the place kind. Hosts are in
// Observability Status.
return Astro.redirect(dataRecordsHref("places"), 308);
---
