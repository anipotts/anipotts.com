---
import { lifeRedirect } from "../../lib/data-routes";
// Retired: every Life section lands on its Data view. People, Projects and
// Places keep their kind filter; Timeline is the records list's recent order.
return Astro.redirect(lifeRedirect(Astro.params.section), 308);
---
