# phase 2 understand report

scope: answers phase 1's open questions and gives the design phase its inputs. five investigations (profiler, props, routers, fixtures, edge) ran in scratch worktrees of `d8ecd618`. a verification pass then rebuilt the base and reran the headline experiments. where they disagree, the verification result is used. line numbers are at `d8ecd618`, whose `apps/admin/src/components` match main. no repo files changed.

status change since phase 1: PR #380 merged at 2026-09-14T13:35:58Z (merge `d1b48160`). deploy run 34850241170 ran `Deploy admin` successfully and skipped www. the harness is now on main, and `Server-Timing` ships in the admin production Worker. whether the header appears behind Access was not checked.

| proof level | meaning                                                                                             |
| ----------- | --------------------------------------------------------------------------------------------------- |
| verified    | causal experiment or direct measurement, reproduced by the verification pass                        |
| proven      | causal experiment or measurement from one investigation. verification checked the source facts only |
| likely      | code read plus partial measurement                                                                  |
| estimate    | built from measured parts, never measured end to end                                                |
| unproven    | still open                                                                                          |

the 1-minute load average ran 3 to 10 during phase 2, from other agents and desktop work. absolute times in verification ran 1.5 to 1.8x those in the investigations, so compare ratios and base-versus-patch deltas.

## headline

| #   | finding                                                                                                                                                                                                                                                                                                                                                  | proof                        |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------- |
| 1   | CLS at 390 and 768 comes from Astryx AppShell's mobile top bar. It mounts at hydration because the server snapshot assumes desktop. The proposed fix renders both on the server and lets CSS choose, which needs an AppShell patch and is untested. Guessing mobile on the server just moves the shift to 1280 (0.1563)                                  | cause verified, fix unproven |
| 2   | the Content settle tail and the `/content` long task are the same render bug. `Updated` is not memoized, so it re-renders 46 dehydrated Timestamp Suspense boundaries one at a time, then React's 300 ms fallback throttle adds the tail. With `React.memo(Updated)` the long task is 0 and the tail is 20 to 35 ms                                      | verified                     |
| 3   | record to library looked bimodal because the harness waited ~15 s on an uncancelled 503 body. The leave flush and server contention are ruled out. A real idle dwell still adds 25 to 50 ms to the next document request, cause unknown                                                                                                                  | verified, cause unproven     |
| 4   | Content group switches can stay in the island through Astryx `LinkProvider`: 0 documents, 0 requests, 42 to 52 ms from click to next frame, one announcement. The leave protocol has to move into the router, because React's root listener calls `preventDefault` before HomeEditor's document listener runs                                            | proven                       |
| 5   | production hashed assets never return 304. The Astro Cloudflare adapter passes `request.url` instead of the Request to `ASSETS.fetch`, so every revalidation is a full 200 plus a Worker invocation. Excluding `/_astro/*` from `run_worker_first` and adding an immutable `_headers` rule removes ~50 requests from every document after the first load | verified                     |

## 1. answers to the phase 1 open questions

understand phase:

| question                                                                                   | answer                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | proof                                                                                                                                                               |
| ------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| what inserts above `#astryx-app-shell-main` at 390 and 768?                                | `div.astryx-app-shell-header[role=banner]`: 56 px at 390, 57 at 768. It mounts in the same commit that removes the inline side nav panel. `EditorialWorkspaceShell.tsx:394` passes `mobileNav={{ breakpoint: "md" }}` without `defaultIsMobile`. Astryx `AppShell.tsx:505-512` reads `useMediaQuery`, whose server snapshot is `false`, and `:760-786` swaps the bar and panel at the hydration commit. None of the phase 1 candidates is the cause: control build A2 removed the rail, sidebar and theme re-reads, and CLS stayed at 0.0664 / 0.0548. A1 (`defaultIsMobile: true`) read 0.0001 / 0.0001 / 0.1563 at 390 / 768 / 1280 in 7 of 7 runs, which isolates the cause but does not fix it. Hydration also rebuilds the side nav: 104 of the 170 nodes parsed outside main before hydration are disconnected by the end                                                                                             | verified (A2 proven)                                                                                                                                                |
| what fills the 330 to 530 ms settle tail on Content library routes?                        | the selective-hydration cascade (next row), then React's `FALLBACK_THROTTLE_MS` 300 ms retry for the lazy `TimestampHoverCard` Suspense boundary in each `Updated` cell (`ContentLibrary.tsx:65-95`, Astryx `Timestamp.tsx:563-576`). The final mutation touches one `<time>` cell, 301 to 302 ms after the fallback burst. Tail for base / B5 memo / B1 no tooltip, measured by verification on a loaded machine: `/content` 666 / 34 / 25, writing 375 / 27 / 17, newsletter 355 / 26 / 15 ms. Operations and Life show no tail only because they render no rows locally                                                                                                                                                                                                                                                                                                                                                  | verified                                                                                                                                                            |
| why does `/content` run a 220 to 240 ms long task, and why does Projects add 75 to 83 ms?  | the same cascade. Mount effects schedule root updates right after hydration (`EditorialApp.tsx:143-145`, `:160-164`, `ContentLibrary.tsx:244-256`, `EditorialWorkspaceShell.tsx:144`, `:311`). `Updated` passes a new `tooltipEntries` array each render, so all 46 dehydrated Timestamp boundaries see changed props. React hydrates them at sync priority and restarts from the root each time. `Intl.DateTimeFormat` constructions: 3,519 on `/content`, 1,044 on work, 306 on writing. With memo: 138 / 72 / 36, exactly 3 per Timestamp. Long task base to B5: `/content` 390 to 0, work 151 to 0, writing 73 to 0, website 66 to 0, newsletter 50 to 0 ms. Growth is super-linear, not quadratic: render work scales about n^1.8 and CPU about n^1.3 to n^1.6. Whether writing or newsletter crosses 50 ms depends on machine speed. Removing single mount setStates (B3, B4) changed nothing                         | verified (B2 to B4 proven)                                                                                                                                          |
| how big are the island props?                                                              | `/content` EditorialApp props attribute: 56,676 B, or 3,626 B gz standalone. It adds 3.7 KB gz to the document and is 32,658 B as plain JSON. It is the same size on every group. The 23 records are serialized 70 times, 3.04 copies each. Record routes carry 16 to 19.4 KB, `/newsletter` 17.1 KB, and AdminShell `navItems` add 8.4 KB to every Operations and Life document. Most HTML weight is row markup, not props: 194 KB of the 257 KB `/content` document is SSR island markup, 107 KB of that class attributes                                                                                                                                                                                                                                                                                                                                                                                                 | verified                                                                                                                                                            |
| is record to library bimodal from contention, the leave flush, or the aborted fetch dwell? | the dwell. `HomeEditor.tsx:493-495` throws on `!ok` without cancelling the chunked 503 body. The request stays open until `AbortSignal.timeout(15000)`, the harness settle waits for it, and so this was the only step clicked after ~15 s. The leave flush is ruled out: navigation starts 2 ms after the click, and native anchors measure the same. Adding `void response.body?.cancel()` took first change from 199 / 186 ms (7 of 7 runs over 100) to 87 / 86 ms (1 of 7 over) at 768 / 1280. Dwell still costs something on its own. Cancel build, first change by dwell: 0 s 81, 4 s 105, 15 s 125 to 129 ms, with TTFB 49 to 53 against 26 to 31. Phase 1's server contention was a symptom of the dwell                                                                                                                                                                                                            | verified. Cause of the idle penalty (workerd, wrangler proxy or OS scheduling) unproven. The 4 s point is n=5 and disagrees with an earlier low-load run (88 vs 88) |
| what does a real draft read cost?                                                          | local fixture only. Every EditorialLayout document makes 18 DO RPCs: `listWritingDrafts` plus 17 `get` at concurrency 4 (`editorial-inventory-projection.ts:320-377`). A private writing record makes 19. Opening a record adds `get`, `historyPage`, `latestPublication` and `readBase`, for 21 to 22 RPCs, and the target's source crosses RPC 3 to 4 times. With a heavy synthetic fixture the fan-out adds 16 to 22 ms TTFB to `/content/new`, `/newsletter` and record documents. Collapsing it to one RPC saves 6 to 8 ms; dropping the full-source projection saves 10 to 12 ms. In production, `readBase` makes 8 serial GitHub requests per record read (token, ref, commit, 4 trees, blob). The token is minted per request, and history refresh and compare repeat the whole read (`HomeEditor.tsx:910`, `:938`). History is 94 to 98.5% of the record API payload: 1.44 MB of 1.46 MB on a 150-revision article | proven for counts. Production latency unproven: 0.4 to 1.2 s per open is a model assuming 50 to 150 ms per GitHub round trip                                        |
| what do production RTT, Access, the edge and revalidation add?                             | from this Mac to EWR (~12 ms RTT), warm: edge only 11.6 to 13.7 ms, Access 302 15.2 to 19.0, Worker-first asset 26.2 to 27.5, Worker plus one D1 read 32.1 to 37.4. A new connection adds 36 to 40 ms. Access fronts every admin path, including `/_astro` and `/favicon.svg`. Hashed assets return a full 200 on `If-None-Match` (section 6). Signed-in estimate for a document switch: first change 110 to 165 ms, plus a 190 to 280 ms script burst                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | public parts proven; signed-in admin figures are estimates                                                                                                          |
| harness follow-ups                                                                         | none have landed. Phase 2 adds four: cancel the 503 body or fix the dwell per step and report dwell sensitivity as its own cell; add a main-thread-quiet metric so `interactive` is not stamped inside a long task; split React retry-timer mutations from real content arrival; after the AppShell fix, assert CLS 0 at 390 and 768 and run the record cells against the fixture editor                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | n/a                                                                                                                                                                 |

design phase questions, with the evidence so far:

| question                                                  | evidence                                                                                                                                                                                                                                                            | carried to                   |
| --------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------- |
| newsletter rows: props or fetch?                          | 4 build-time rows, 1.9 KB attr, 0.6 KB gz, no private fan-out (`pages/newsletter.astro:6-21`). A fetch costs a round trip to save fewer bytes than its own headers                                                                                                  | section 4, default props     |
| merge Operations and Life into one root?                  | not needed. A shared ESM router module reaches both roots (`@astrojs/react` hydrates one root per island, and `StaticHtml.shouldComponentUpdate` returns `false`). Merging is the bigger change                                                                     | section 3                    |
| router seam over `LinkProvider`                           | the seam exists, needs no Astryx patch, and the Content prototype worked                                                                                                                                                                                            | section 3                    |
| where do table, row and record skeletons live?            | Astryx `Table` has only `emptyState` (`src/Table/types.ts:639`)                                                                                                                                                                                                     | section 5                    |
| denied as a first-class state                             | still folded into other states in Operations, the editor and `/content` 401s                                                                                                                                                                                        | section 8                    |
| does localStorage draft recovery count as a client cache? | correction to phase 1, which said recovery survives logout: in-app sign out clears it (`Logout.tsx:67`). Access session expiry or going straight to `/cdn-cgi/access/logout` would skip that clear (inferred). sessionStorage `admin:navigation:*` is never cleared | section 10                   |
| Server-Timing before #380 merges                          | #380 merged and admin deployed, so the question now applies to production                                                                                                                                                                                           | section 10                   |
| asset policy                                              | adapter cause found, options measured locally                                                                                                                                                                                                                       | sections 6 and 10            |
| which bundle cuts ride along                              | the new render and payload levers pay off more than most byte cuts                                                                                                                                                                                                  | section 7                    |
| cross-workspace switches                                  | 6 to 19 new JS files and a different layout (EditorialLayout vs AdminLayout). The definition of done covers in-workspace only                                                                                                                                       | section 9, default documents |
| Operations automatic refresh announcing "Loading"         | confirmed: `ObservabilityWorkspace.tsx:338` sets Button `isLoading={refreshing}`                                                                                                                                                                                    | section 8                    |
| inbox 10 s poll in scope?                                 | no new evidence                                                                                                                                                                                                                                                     | default stays out of scope   |

## 2. per-workspace architecture today

| surface                                      | islands                                                                                                    | data sources                                                                                                                                                                                                                                  | navigation today                                                                                                                                                                                                 | what a switch refetches                                                                                                                   | blockers for an in-island switch                                                                                                                                                                                                                                                                      |
| -------------------------------------------- | ---------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Content library `/content`, `?group=`        | 1 root: EditorialApp owns shell, library, breadcrumbs and the lazy editor (`EditorialLayout.astro:92-100`) | SSR `loadEditorialInventory` (18 DO RPCs), projected into 5 groups plus `searchEntries` (`content/index.astro:5-17`, `EditorialLayout.astro:50-56`)                                                                                           | native `<a>` in SideNavItem. ContentLibrary filters use pushState plus `editorial:library-state` (`ContentLibrary.tsx:256-273`)                                                                                  | the document (8 to 21 ms SSR CPU), 18 RPCs, ~47 JS and 2 to 3 CSS as full 200s, full hydration, CLS at 390 and 768, the Timestamp cascade | no data blocker. Needs `LinkProvider`, `selectedGroup` as state, scroll restore, and a heading ladder fix (`EditorialApp.tsx:219-231` renders "Content" for work and systems)                                                                                                                         |
| Newsletter `/newsletter`                     | 1 root: EditorialApp                                                                                       | SSR `getCollection('newsletterDrafts')`, plus the full inventory again for `searchEntries`                                                                                                                                                    | native `<a>`                                                                                                                                                                                                     | as above, plus the fan-out, which only feeds palette search                                                                               | rows are not in `/content` props. `area` is a prop (`EditorialLayout.astro:39-41`). Title changes per area                                                                                                                                                                                            |
| Newsletter draft `/newsletter/[slug]`        | 1 root                                                                                                     | SSR review build (`newsletter/[slug].astro`)                                                                                                                                                                                                  | native `<a>`                                                                                                                                                                                                     | the document                                                                                                                              | no client read path. Rows are review-only (`content-decision.ts`)                                                                                                                                                                                                                                     |
| Content record `/content/<collection>/<id>`  | 1 root. HomeEditor via `React.lazy` plus Suspense (`EditorialApp.tsx:267-285`)                             | SSR entry, plus the inventory twice (`[id].astro:19`, `EditorialLayout.astro:53`). `review` and rendered Markdown are built but never shown (`EditorialApp.tsx:286`, `:345`). Client `record` API call on editor mount (`HomeEditor.tsx:493`) | native `<a>`, intercepted by HomeEditor's document click listener, which flushes then calls `location.assign` (`HomeEditor.tsx:250-331`, `editorial-navigation.ts:3-5`). Record views use pushState (`:352-418`) | the document, the 408 KB gz editor chunk again, the record API (3 RPCs, plus 8 GitHub requests in production), 2 announcements            | leave guard must move to the router. Editor key omits kind (`EditorialApp.tsx:281`). A deep-linked record has no library rows. The 404 and 503 document statuses need client states. `returnTo` is read once (`HomeEditor.tsx:163-171`)                                                               |
| Operations `/operations/observability?view=` | 2 roots: AdminShell (`AdminLayout.astro:81-94`) and ObservabilityWorkspace nested in its slot              | one SSR snapshot covers all 7 views. Client refresh through `/api/admin/observability` (`ObservabilityWorkspace.tsx:120-217`)                                                                                                                 | the view dropdown uses pushState plus `admin:workspace-navigation` (`:78-94`). The side nav is native `<a>` in the other root (`AdminShell.tsx:120-140`)                                                         | the document. Once a capability is wired, also a duplicate snapshot read at mount (`:209`)                                                | the side nav sits in another root, so the router must be a shared module. ObservabilityWorkspace only listens for popstate                                                                                                                                                                            |
| Life `/life`, `/life/[section]`              | 2 roots: AdminShell and LifeWorkspace                                                                      | one SSR `readPersonalContext(lifeSectionRead(section))` per section, private no-store. No transport on main                                                                                                                                   | side nav in AdminShell, "Your views" ListItems in LifeWorkspace (`LifeWorkspace.tsx:714-838`)                                                                                                                    | the document. No new JS or CSS                                                                                                            | no client read path: no page passes `reader` (`:628-636`). The Codex owner reader (`369fd461`, not on main) allows only status, sources, search and get, so timeline, preview and activity would come back denied. A JSON endpoint would be a new private-data surface. Reads can be up to 1 MiB each |
| Life `/life/health`, `/life/aesthetics`      | health: 2 roots (LifeSupportingView). aesthetics: AdminShell only                                          | health: D1 knowledge read. aesthetics: static                                                                                                                                                                                                 | native `<a>`                                                                                                                                                                                                     | the document                                                                                                                              | a different component and data source from the sections. Unknown sections return bare "Not found" text                                                                                                                                                                                                |

## 3. the router seam

what exists:

| mechanism                          | where                                                                                                                  | what it does                                                                                                                                                                                     | notes                                                                                                                                                                                                                                                                                                                                        |
| ---------------------------------- | ---------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Astryx `LinkProvider`              | `@astryxdesign/core/Link`. `useLinkComponent` resolves `as ?? ctx?.component ?? 'a'`                                   | renders every Astryx link: Button, IconButton, SideNavItem, SideNavHeading, BreadcrumbItem, Item and ListItem, Tab, Token, ClickableCard, TreeListItem, TopNav and NavMenu items, Markdown links | not mounted anywhere in `apps/admin/src`. Astryx passes its own `onClick` (drawer close, disabled guard) into the link, so a router link must call it first. Not covered: the AppShell skip link (`AppShell.tsx:815-821`), CommandPalette, raw `<a>` (`ContentLibrary.tsx:365-382` and Astro components). The pnpm patch does not touch Link |
| pushState router 1                 | `ContentLibrary.tsx:244-273`                                                                                           | search, status, sort and sections. Emits `editorial:library-state`                                                                                                                               | has its own popstate listener                                                                                                                                                                                                                                                                                                                |
| pushState router 2                 | `ObservabilityWorkspace.tsx:78-94`                                                                                     | the view. Emits `admin:workspace-navigation`                                                                                                                                                     | has its own popstate listener                                                                                                                                                                                                                                                                                                                |
| pushState router 3                 | `HomeEditor.tsx:352-418`                                                                                               | record views and panels                                                                                                                                                                          | its popstate handler treats any URL as view `edit` (`:410-418`)                                                                                                                                                                                                                                                                              |
| `admin:navigate`                   | `lib/editorial-navigation.ts:1-14`, dispatched by `AdminCommandPalette.tsx:322-325`                                    | cancelable event. HomeEditor cancels it, flushes, then calls `location.assign`                                                                                                                   | used by the palette only                                                                                                                                                                                                                                                                                                                     |
| HomeEditor document click listener | `HomeEditor.tsx:295-324`                                                                                               | intercepts same-origin left clicks, flushes, then calls `location.assign`                                                                                                                        | returns early when `defaultPrevented` is set (`:297`)                                                                                                                                                                                                                                                                                        |
| imperative exits                   | `NewWriting.tsx:190`, `OperatorWorkTable.tsx:88`, `Logout.tsx:17`, EditorialApp reload, `AdminHome.astro:317`, `:374`  | `location.assign` or reload                                                                                                                                                                      | skip any leave guard                                                                                                                                                                                                                                                                                                                         |
| sync listeners                     | `AdminShell.tsx:53-62`, `EditorialWorkspaceShell.tsx:137-168` (sessionStorage destinations) and `:311` (librarySearch) | re-read the URL on popstate and on both custom events                                                                                                                                            | a new router must keep emitting both events                                                                                                                                                                                                                                                                                                  |
| `beforeunload`                     | `HomeEditor.tsx:709-721`                                                                                               | fires on document unload only                                                                                                                                                                    | pushState never triggers it                                                                                                                                                                                                                                                                                                                  |

leave protocol facts:

| fact                                                                                                                                                                                | proof                                                                                                                      |
| ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------- |
| a provider link's `preventDefault` runs at the island root before HomeEditor's document listener, so that listener skips its flush. 15 of 15 clicks saw `defaultPrevented === true` | proven                                                                                                                     |
| an in-island route away from a dirty record would unmount the editor without flushing                                                                                               | likely: inferred from the event order and `:297`. The editor never mounted locally; the fixture editor makes this testable |
| popstate cannot be cancelled, and HomeEditor's handler reads a library URL as a record view                                                                                         | verified (source)                                                                                                          |
| the loaded-editor flush path (a real save before `location.assign`) has never been measured                                                                                         | unproven                                                                                                                   |

the minimal unifying seam is one ESM module, `lib/admin-router.ts`. Every island imports it, so both AdminLayout roots share one instance.

| part                                  | job                                                                                                                                                                                                                                                                                                                                                                                                      |
| ------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| resolver registry                     | each island claims the URLs it can render in place. EditorialApp: groups now, newsletter and records later. ObservabilityWorkspace: views. LifeWorkspace: sections, once a reader exists. Unclaimed URLs fall back to `location.assign`                                                                                                                                                                  |
| `AdminLink` and `router.commit(href)` | `AdminLink` is the `LinkProvider` component in each root. It calls `props.onClick` first, then returns on `defaultPrevented`, modifier keys, non-left buttons, `download`, a target other than `_self`, or a cross-origin URL. Otherwise it strips `to` and hands the URL to the router. `commit` replaces `commitAdminNavigation` and covers the NewWriting and OperatorWorkTable exits and the palette |
| leave-guard registry                  | generalizes `admin:navigate`. HomeEditor registers its flush and held-leave copy. The router checks guards on link clicks, `commit` and popstate. If a popstate leave is held, it pushes the record URL back while the editor is still mounted                                                                                                                                                           |
| history entries                       | store `{ scrollTop }` of `#astryx-app-shell-main`, which AppShell `height=fill` scrolls and browser restoration ignores. The router owns popstate and dispatches only to the active resolver                                                                                                                                                                                                             |
| title and focus policy                | nav switches keep focus on the nav item and reset scroll. Record open focuses the h1 or title field once the editor resolves. `document.title` is set per route (HomeEditor sets it for writing only, `:676-686`)                                                                                                                                                                                        |
| announcements                         | the router adds none. Each destination keeps one status region it owns, such as the `ContentLibrary.tsx:529` count region for library switches                                                                                                                                                                                                                                                           |
| events                                | keep emitting `editorial:library-state` and `admin:workspace-navigation`                                                                                                                                                                                                                                                                                                                                 |

sign out, auth routes and cross-layout switches stay document loads. Deep links keep working because the server still renders every URL.

build order: first the router module, `AdminLink` and Content groups (proven). Then newsletter rows, then record open and close behind the guard (needs the fixture editor), then Operations views. Life waits for an approved reader. Extend the `HomeEditor.navigation` tests to cover pushState exits and popstate while dirty.

## 4. data plan inputs

props today, from a warm local owner build with published fixtures:

| route                          | island props attr   | attr gz       | plain JSON     | SSR island markup | document / gz                |
| ------------------------------ | ------------------- | ------------- | -------------- | ----------------- | ---------------------------- |
| `/content` and every `?group=` | 55.3 KB             | 3.6 KB        | 31.9 KB        | 38 to 194 KB      | 101 to 257 / 13.9 to 18.2 KB |
| `/newsletter`                  | 17.1 KB             | 2.8 KB        | 10.2 KB        | 59.9 KB           | 84.9 / 13.7 KB               |
| record routes (23)             | 16.0 to 19.4 KB     | 2.6 to 3.2 KB | 9.4 to 11.2 KB | 34 to 48 KB       | 56 to 74 / 12.3 to 13.2 KB   |
| Operations                     | 8.4 + 3.4 KB        | 1.3 + 0.4 KB  | 4.1 + 1.8 KB   | 36.5 KB           | 55.0 / 11.3 KB               |
| Life sections                  | 8.4 + 0.1 to 0.2 KB | 1.3 + 0.1 KB  | 4.2 KB         | 28 to 45 KB       | 45 to 62 / 10.7 to 12.3 KB   |

duplication and dead weight:

| item                                                                                   | bytes                                | used for                                                                                         |
| -------------------------------------------------------------------------------------- | ------------------------------------ | ------------------------------------------------------------------------------------------------ |
| subset groups (website, work, writing, systems) re-copying the pages rows              | 11.9 KB plain                        | client switching only. SSR markup is byte-identical without them                                 |
| `searchEntries` on every editorial document                                            | 15.0 KB attr, 2.2 KB gz              | the palette only. It also forces the 18-RPC fan-out on `/newsletter`, `/content/new` and records |
| constant `capabilities` per record                                                     | 1.6 KB plain                         | nothing                                                                                          |
| `publishedUpdated`, equal to `updated` in 23 of 23 records (no private drafts locally) | 1.6 KB plain                         | only differs when drafts exist                                                                   |
| `review` plus the rendered Markdown slot on editable record routes                     | 0.8 to 4.2 KB attr, plus 0 to 3.2 KB | never rendered                                                                                   |
| AdminShell `navItems`                                                                  | 8.3 KB attr, 1.2 KB gz               | static data                                                                                      |

normalized `/content` props: 19.8 KB attr with each record once. The slim shape drops the constant capabilities and duplicate `publishedUpdated` for 14.0 KB attr, 2.3 KB gz. At 149 records, 347 KB today becomes 88 KB slim. Props cost the server ~0.025 ms per KB, while each row costs ~0.57 ms and 6.8 KB. So dedupe is a fix for scaling, not today's speed. The client parses the 55 KB of props in 0.18 ms. (proven)

what each switch needs:

| switch                                       | data                                                 | source today            | plan input                                                                                                                                       |
| -------------------------------------------- | ---------------------------------------------------- | ----------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------ |
| Content group to group                       | target rows                                          | props, 3 copies         | zero requests. Serialize records once and derive groups from collection on the client (the `editorialInventorySearch` map already exists)        |
| Content to Newsletter                        | 4 rows                                               | not in `/content` props | add to props, 1.9 KB attr                                                                                                                        |
| library to record                            | identity, title, kind                                | the row href            | the editor chunk and the `record` API, nothing else                                                                                              |
| record to library, arriving from the library | full rows                                            | island memory           | zero requests                                                                                                                                    |
| record to library, from a deep link          | full rows (updated, section, status, changed fields) | not on record documents | slim records in record props (15.0 KB attr, less than today's 16.1 KB with searchEntries), or a document load. No inventory JSON endpoint exists |
| Operations view to view                      | one snapshot                                         | props                   | zero requests                                                                                                                                    |
| Life section to section                      | a separate private read per section                  | SSR only                | fetch through an approved reader, or stay a document load                                                                                        |
| palette search on non-library documents      | search entries                                       | the layout fan-out      | derive from rows already in the island, or load when the palette opens                                                                           |

prefetch without background polling (only on user intent, never on idle or a timer):

| candidate                                             | trigger                                                                           | head start                                                                                                                              | cost                                                                                                                                         |
| ----------------------------------------------------- | --------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------- |
| HomeEditor chunk `import()`                           | pointerdown or hover on a record row                                              | takes the chunk wait off the click (locally the chunk finishes 198 to 220 ms after navigation)                                          | 408 KB gz once per session, free after the first open with immutable caching                                                                 |
| `record` API read                                     | pointerdown only, since hover would fire private reads as the pointer passes over | today the read starts 85 to 100 ms after the chunk finishes (`HomeEditor.tsx:493`). In production the read carries 8 GitHub round trips | one read per intent, discarded on cancel. The editor must use it instead of fetching again                                                   |
| Content groups, newsletter, Operations views          | none                                                                              | data already in memory                                                                                                                  | 0                                                                                                                                            |
| Life sections                                         | blocked until a reader exists                                                     | n/a                                                                                                                                     | a private read per intent, so pointerdown only                                                                                               |
| document prefetch (Astro prefetch, speculation rules) | hover                                                                             | a TTFB head start                                                                                                                       | admin documents are `private, no-store`, and reuse of a prefetched response is unverified. Prerender runs page JS, including mount refreshes |

memory-only session cache implications:

| implication                                                                                                                                                                                                                                                               | proof                                   |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------- |
| island memory dies with the document. Cross-workspace switches, reload, auth routes and sign out (`Logout.tsx:72` calls `location.assign`) all clear it with no extra code                                                                                                | likely                                  |
| rows in memory go stale after an in-island edit. Closing a record must write the saved `updated`, status and changed fields back into the inventory, or the library shows the pre-edit row                                                                                | likely                                  |
| Life reads can be up to 1 MiB each, and an Operations snapshot ~200 KB attr at its caps, so any per-section memory needs a bound                                                                                                                                          | proven sizes                            |
| no localStorage cache is added. Current storage: editorial recovery in localStorage (cleared by in-app sign out, `Logout.tsx:67`); theme and sidebar keys; sessionStorage `admin:navigation:*` URL paths (`EditorialWorkspaceShell.tsx:140-150`), not cleared on sign out | verified (source)                       |
| immutable bundles stay in the disk cache after sign out. They hold code, not data. `Clear-Site-Data: "cache"` on sign out would remove them                                                                                                                               | proven for headers; the policy is Ani's |

## 5. skeleton inputs

loaded layouts that need a matching loading state:

| loaded layout                                                                       | component                                                    | loading today                                                                                                                        | needed under in-island switching                                                                            |
| ----------------------------------------------------------------------------------- | ------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------- |
| Content library table (24 rows, 44 elements and 2 Timestamps per row)               | `ContentLibrary.tsx:547-636`                                 | none (SSR)                                                                                                                           | none for group switches, which render synchronously. A row-shaped state only for a deep-link inventory read |
| Content empty result                                                                | `ContentLibrary.tsx:638-663`                                 | replaces the Table, header included, which shifts layout                                                                             | keep the header and put the empty state in the body                                                         |
| newsletter table                                                                    | same component                                               | none                                                                                                                                 | none if rows are in props                                                                                   |
| record editor: heading, action toolbar, Edit and Preview tabs, fields               | `HomeEditor.tsx:1040-1081`, `:1566-1575`                     | Suspense `AdminSkeleton` (`EditorialApp.tsx:268-284`), then HomeEditor's own (`HomeEditor.tsx:735`). Form bars only, 2 announcements | one editor-shaped state covering chunk and read, with one announcement                                      |
| version history rows                                                                | `HomeEditor.tsx:1980-1984`                                   | a status sentence plus `AdminSkeleton kind=history`                                                                                  | shaped like revision rows                                                                                   |
| compare with website                                                                | `HomeEditor.tsx:1372`                                        | Spinner                                                                                                                              | diff-shaped, or pending on the button                                                                       |
| preview                                                                             | `HomeEditor.tsx:1589-1626`, `SavedArticlePreview.tsx:88-121` | Spinner, then status text. ~340 px of bars for an 800 px iframe                                                                      | a box the size of the iframe, one announcement                                                              |
| review                                                                              | `HomeEditor.tsx:1785`                                        | an inserted sentence that shifts content                                                                                             | shown in place                                                                                              |
| Operations inventory table (machines, loops, coverage)                              | `ObservabilityWorkspace.tsx:440` (`isInventory` at `:221`)   | none. Refresh shows Button `isLoading`                                                                                               | none for view switches, since the snapshot is in memory                                                     |
| Operations signal table (includes metrics; the rest of the view mapping not traced) | `ObservabilityWorkspace.tsx:521`                             | none                                                                                                                                 | none                                                                                                        |
| Operations service detail                                                           | `ObservabilityWorkspace.tsx:592-640`                         | none                                                                                                                                 | none                                                                                                        |
| Operations empty                                                                    | `ObservabilityWorkspace.tsx:572-591`                         | replaces the Table                                                                                                                   | keep the header                                                                                             |
| Life explorer table                                                                 | `LifeWorkspace.tsx:294`                                      | `AdminSkeleton kind=history` (`:566`) plus the count region (`:275`), 2 announcements                                                | table-shaped, one announcement, and keep the pagination toolbar, which unmounts today (`:577-603`)          |
| Life record detail                                                                  | `LifeWorkspace.tsx:614`                                      | `AdminSkeleton kind=history`                                                                                                         | shaped like the detail                                                                                      |
| Life empty                                                                          | `LifeWorkspace.tsx:376-388`                                  | replaces the Table                                                                                                                   | keep the header                                                                                             |
| Life activity                                                                       | `LifeActivityView.tsx:55`                                    | a status sentence                                                                                                                    | unchanged until the reader allows activity                                                                  |
| command palette results                                                             | `AdminCommandPalette.tsx:304`                                | status text plus the input Spinner, likely 2 announcements                                                                           | one                                                                                                         |

Astryx constraints:

| constraint                                                                                                     | where                                             | effect on the design                                                                                                                               |
| -------------------------------------------------------------------------------------------------------------- | ------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------- |
| `Table` takes `emptyState` (ReactNode or false) and has no loading prop                                        | `src/Table/types.ts:639`                          | table and row skeletons need an admin wrapper that renders real `Table` columns with placeholder rows, or an Astryx patch that adds a loading prop |
| the `Skeleton` primitive sits at 0.25 opacity with a `steps(10)` pulse that starts after ~1 s                  | `src/Skeleton/Skeleton.tsx:52-81`                 | waits under a second show static bars                                                                                                              |
| reduced motion sets `animation: none` on every themed descendant                                               | `admin-interactions.css:24-31`                    | skeleton pulses and any view transition must fall back to static. It also cancels Button's delayed spinner reveal (inferred)                       |
| every Button renders a hidden `role=status`                                                                    | Astryx `Button.tsx:731-733`                       | count announcements by live-region text changes, not by nodes                                                                                      |
| Timestamp wraps a lazy `TimestampHoverCard` in Suspense per cell and builds 3 `Intl.DateTimeFormat` per render | `src/Timestamp/Timestamp.tsx:459-479`, `:563-576` | any row that renders Timestamp gets the 300 ms throttle unless it is memoized or the hover card moves up to one per table                          |
| each `isLive` Timestamp starts its own `setInterval` (1 s to 5 min) with no hidden check                       | `Timestamp.tsx:467-479`                           | 46 timers on `/content`. They are timers, not network. Cost unmeasured                                                                             |
| `Text` `useTruncation` sets state from ref callbacks during commit (284 calls on `/content`)                   | profiler count                                    | placeholder rows built from `Text` add sync re-renders and layout reads                                                                            |
| the AppShell mobile bar is chosen from the `useMediaQuery` server snapshot                                     | `src/AppShell/AppShell.tsx:505-512`, `:760-786`   | no skeleton can hide the CLS. It needs CSS to choose the bar                                                                                       |

## 6. network and caching inputs

public measurements from this Mac to EWR, median ms (proven):

| path                                        | warm TTFB                                                          | cold total | note                                                                       |
| ------------------------------------------- | ------------------------------------------------------------------ | ---------- | -------------------------------------------------------------------------- |
| edge only `/cdn-cgi/trace`                  | 11.6 to 13.7                                                       | n/a        | RTT plus edge                                                              |
| admin Access 302 (document or `/_astro`)    | 15.2 to 19.0                                                       | 63 to 66   | Access adds 3 to 5 ms. It applies to every path and runs before the Worker |
| www hashed asset via Worker and ASSETS      | 26.2 to 27.5                                                       | 73 to 75   | Worker-first adds ~15 ms per asset                                         |
| www document                                | 28.0 to 43.8                                                       | 75 to 80   |                                                                            |
| www `/api/health` (Worker plus 1 D1 read)   | 32.1 to 37.4                                                       | 82         |                                                                            |
| 50 multiplexed requests after the handshake | edge 44 to 48, Access 59 to 62, Worker 107 to 156 (p90 174 to 196) | n/a        | about one document's script burst                                          |

a new connection adds 36 to 40 ms (DNS ~3, TCP 11 to 12, TLS 19 to 23), paid once per connection. A 312 KB image downloads in 13 to 25 ms on a warm connection, so request count and Worker invocations matter more than bytes.

asset revalidation (verified):

| fact                                                                                                                                                         | evidence                                                                   |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------- |
| production www answers a matching `If-None-Match` on a hashed CSS file with a full 200 and `public, max-age=0, must-revalidate`                              | direct probe                                                               |
| cause: `@astrojs/cloudflare@12.6.13` `dist/utils/handler.js` returns `env.ASSETS.fetch(request.url.replace(/\.html$/, ""))`, which drops the request headers | patched to `new Request(url, request)`: 304, 0 B. Unpatched: 200, 51,307 B |
| every asset goes through that path because both `wrangler.toml` files set `run_worker_first = true` and neither app has `public/_headers`                    | source                                                                     |
| admin uses the same adapter and routing, and every `/_astro` file is in `manifest.assets` (89 in the local owner build; the count varies by build)           | source. Signed-in admin not probed                                         |
| phase 1 blamed wrangler for the full 200. The adapter causes it                                                                                              | local variants v2 and v3 returned 304 under wrangler                       |

production switch estimate (not measured signed in):

| part                                                  | loopback                   | production                                                                                                               |
| ----------------------------------------------------- | -------------------------- | ------------------------------------------------------------------------------------------------------------------------ |
| document TTFB                                         | 15 to 41                   | +~30, before real DO and GitHub reads                                                                                    |
| render-blocking CSS round trip                        | ~0                         | +27 to 35                                                                                                                |
| first change                                          | 64 to 103                  | ~110 to 165 total                                                                                                        |
| ~47 eager scripts in 2 discovery waves, all full 200s | only request count visible | ~190 to 280 before parse and compile. 296 KB (Content) to 713 KB (record) downloaded again, 50 billed Worker invocations |

caching options:

| option                                                                                                                                            | local result                                                                   | privacy                                                                                                                                                                                              |
| ------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `run_worker_first = ["/*", "!/_astro/*"]` plus `apps/admin/public/_headers` with `/_astro/*  Cache-Control: private, max-age=31536000, immutable` | immutable, 304 on revalidation, no Worker invocation per asset (v2)            | Access still gates the first fetch. The Worker never guarded assets: the adapter returns before middleware, and `src/worker.ts` only reports the runtime contract. `private` keeps shared caches out |
| `_headers` alone, keeping `run_worker_first = true`                                                                                               | the immutable header applies, but the first load still invokes the Worker (v1) | same                                                                                                                                                                                                 |
| pnpm patch so the adapter forwards the Request                                                                                                    | 304s, but ~50 requests per document remain (v3)                                | no change                                                                                                                                                                                            |
| `build.assetsPrefix` pointing to a public host                                                                                                    | not tested                                                                     | moves admin code outside Access. Reject                                                                                                                                                              |
| www: the same exclusion with `public, max-age=31536000, immutable`                                                                                | not deployed                                                                   | public site                                                                                                                                                                                          |

modulepreload: Astro 5.18.2 emits none. Its only setting is `modulePreload: { polyfill: false }` for SSR (`core/build/static-build.js:170`). Injecting `<link rel=modulepreload>` for each island's static import graph into AdminLayout, from build output, would pull the 44 second-level chunks into the first wave. That saves an estimated ~27 to 30 ms per wave in production. Keep Cloudflare Early Hints off for admin: cached `Link` hints may reach unauthenticated visitors before Access runs (ordering unverified). The bundle privacy scan of 71 JS and 7 CSS files found no emails, tokens, AUD, Cloudflare account, D1 or GitHub App ids. Bundles do reveal route and API names, which argues for `private`.

other network facts:

| fact                                                                                                                                           | proof                           |
| ---------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------- |
| a document request after an idle dwell is slower. Cancel build, first change: 0 s 81, 4 s 105, 15 s 125 ms, with TTFB up ~20 ms. Cause unknown | effect verified, cause unproven |
| admin documents send `Cache-Control: private, no-store` to principals (`middleware.ts:98`, `:110`, `:133`)                                     | proven                          |
| only a signed-in production run can check the Access and admin Worker parts of the estimate                                                    | n/a                             |

## 7. bundle levers

| lever                                                                                               | cost today                                           | new evidence                                                                                                    | proof               |
| --------------------------------------------------------------------------------------------------- | ---------------------------------------------------- | --------------------------------------------------------------------------------------------------------------- | ------------------- |
| `React.memo(Updated)` with stable tooltip props (`ContentLibrary.tsx:65-95`)                        | 240 to 390 ms long task and a 300 ms tail on Content | long task to 0, script 281 to 61 ms (455 to 95 on a loaded machine), commits 51 to 5                            | verified            |
| Astryx Timestamp: cache `Intl.DateTimeFormat` per locale and options (`formatInstant.js:118`)       | 138 constructions per SSR render, ~30 µs each        | `/content` Worker CPU 20.33 to 17.0 ms (-16%), -37% at 149 rows. Still 0.07 ms per client render after the memo | proven              |
| one hover card per table instead of a lazy Suspense per cell                                        | 300 ms throttle whenever a cell falls back           | B2 (eager card) removes the throttle but not the cascade                                                        | proven              |
| skip the layout inventory on record, `/newsletter` and `/content/new`                               | 18 RPCs plus projection per document                 | record document CPU 8.17 to 5.5 ms. Fixture TTFB 16 to 22 ms lower                                              | proven              |
| one-RPC inventory returning stored summaries                                                        | 18 RPCs of full sources                              | batching saves 6 to 8 ms; skipping source projection saves 10 to 12                                             | proven              |
| record API returns history metadata only, fetching source on Restore or Download                    | 94 to 98.5% of the payload                           | 1.44 MB of 1.46 MB on the heavy fixture. The list renders only revision and time (`HomeEditor.tsx:1986-2033`)   | proven              |
| start the record read in parallel with the editor chunk                                             | read starts 85 to 100 ms after the chunk finishes    | locally the read is under 6% of the wait. In production it is most of it                                        | proven locally      |
| reuse the GitHub token and read content in one call. History and compare skip `readBase`            | 8 serial requests per read, repeated                 | mock transport count                                                                                            | proven (count)      |
| props: records once, slim shape, newsletter rows added, `review` and slot dropped on editor records | 55.3 KB attr on `/content`                           | 14.0 KB attr. Helps scaling only (0.025 ms per KB)                                                              | verified sizes      |
| move `navItems` into the AdminShell bundle                                                          | 8.3 KB attr per Operations and Life document         | static data                                                                                                     | proven              |
| defer CodeMirror until the source tab opens (`HomeEditor.tsx:1900`, `:2146`)                        | ~173 KB gz of the 408 KB gz editor chunk             | the chunk gates the local editor wait (chunk done 198 to 220 ms, editor 352 ms)                                 | carried             |
| split `MAX_SOURCE_BYTES` and the id schema out of `@anipotts/content/editorial/source`              | 59 KB gz on every Content route                      | none                                                                                                            | carried             |
| modulepreload injection                                                                             | a second discovery wave per document                 | section 6                                                                                                       | likely              |
| immutable asset caching                                                                             | ~50 full 200s per document                           | section 6                                                                                                       | mechanism verified  |
| formatjs `no-parser` build with precompiled messages                                                | ~26.4 KB gz                                          | none                                                                                                            | carried             |
| Phosphor regular-only imports                                                                       | ~95 KB raw                                           | none                                                                                                            | carried             |
| marked dedupe                                                                                       | 41 to 43 KB raw                                      | none                                                                                                            | carried             |
| per-workspace theme split                                                                           | 14.7 to 15.3 KB JS each, plus 32 KB gz CSS           | none                                                                                                            | carried             |
| server Worker size (astro-icon `ph: ['*']`, HomeEditor server copy, Shiki)                          | 23.9 MB raw                                          | cold start only. A cold isolate's first `/content` took 128 ms against 26 ms warm and streamed 46 fallbacks     | proven (single run) |

## 8. constraint violations to fix alongside

| constraint                                     | violation                                                                                                                                                                                    | where                                            | proof                                                             |
| ---------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------ | ----------------------------------------------------------------- |
| no hidden polling                              | HomeEditor's publication poll runs every 4 s, with no hidden check and no backoff on failure: 15 polls per hidden minute                                                                     | `HomeEditor.tsx:617-649`                         | proven (jsdom), verified (source)                                 |
| no hidden polling                              | LifeActivityView polls every 1 s (100 ms while catching up) with a 5 s ceiling, no hidden check, and retries denied forever: 61 reads per hidden minute when ready, 13 when denied           | `LifeActivityView.tsx:17-51`                     | proven (jsdom), verified (source)                                 |
| no duplicate reads                             | once a capability is wired, Operations refreshes at mount and repeats the SSR read. Scheduling the first refresh 60 s out removed it in 5 of 5 runs                                          | `ObservabilityWorkspace.tsx:209`                 | proven (fixture)                                                  |
| hidden timers (scope question)                 | 46 `isLive` Timestamp intervals on `/content`, with no hidden check                                                                                                                          | Astryx `Timestamp.tsx:467-479`                   | likely. Not network                                               |
| failed or denied reads show their real state   | the editor shows 401, 403, 404 and network failure all as "Couldn't load this draft."                                                                                                        | `HomeEditor.tsx:493-610`                         | verified (source)                                                 |
| failed or denied reads show their real state   | Operations shows 401, 403, redirect and timeout all as "disconnected"                                                                                                                        | `ObservabilityWorkspace.tsx:144-150`, `:180-186` | carried                                                           |
| failed or denied reads show their real state   | denial on `/content` and `/newsletter` renders a raw JSON 401 as the page                                                                                                                    | `middleware.ts:72`                               | carried                                                           |
| failed or denied reads show their real state   | an unknown Life section returns bare "Not found"                                                                                                                                             | `pages/life/[section].astro`                     | verified (source)                                                 |
| resource hygiene                               | error bodies are never cancelled, so each failed read holds a fetch open for 15 s                                                                                                            | `HomeEditor.tsx:455-477`, `:493-495`             | verified                                                          |
| one announcement per wait                      | record open announces twice: the Suspense fallback, then HomeEditor's skeleton                                                                                                               | `EditorialApp.tsx:270`, `HomeEditor.tsx:735`     | carried, measured                                                 |
| one announcement per wait                      | Operations' automatic refresh announces "Loading" through Button `isLoading`                                                                                                                 | `ObservabilityWorkspace.tsx:338`                 | verified (source)                                                 |
| one announcement per wait                      | a Life client read announces through both the skeleton and the count region                                                                                                                  | `LifeWorkspace.tsx:275`, `:565-576`              | likely                                                            |
| one announcement per wait                      | writing preview, preview failure, history open, palette bootstrap                                                                                                                            | phase 1 code reads                               | carried                                                           |
| a11y                                           | the Projects and Systems groups render h1 "Content"                                                                                                                                          | `EditorialApp.tsx:219-231`                       | verified                                                          |
| a11y                                           | two side nav items carry `aria-current=page` on Content pages                                                                                                                                | router probe                                     | unproven                                                          |
| Server-Timing on unauthenticated app responses | reached admin production with #380 (run 34850241170)                                                                                                                                         | `middleware.ts`                                  | deploy verified. The production header behind Access is unchecked |
| docs                                           | `docs/local-development.md:95` says the preview shares Durable Object state with astro dev. Only D1 is shared: the dev DO lives in Miniflare under `.local/editorial-drafts` as `local-home` | fixtures                                         | verified (source)                                                 |
| out of scope, security                         | prerendered www pages (`/`, `/writing`, `/work`) ship without CSP, X-Frame-Options, HSTS or nosniff, while the on-demand 404 has all four. Likely the same early asset return in the adapter | edge probe                                       | proven (headers), cause likely                                    |

enabling fix, not a violation: a local-owner-only editorial fixture mode, ~60 lines in `editorial-server.ts`. It uses DO storage under a dedicated name, bundled checkout content as the base, and has publishing off. Every editorial read worked without secrets, and a release build contained none of it (the leak scan passed on 601 files). Add its DO name to `LOCAL_OWNER_MARKERS` in `scripts/ci/admin-local-owner-leak.mjs`. The scratch patch is `scratchpad/speed/fixtures/fixture-mode-scratch.patch`; do not carry its batch mode, which is ungated. (proven)

## 9. inputs for the design judge panel

| approach                           | what it means                                                                                                                                                                                                                                                                                                          |
| ---------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| (a) in-island pushState            | `LinkProvider` plus a shared router. Each workspace renders its destinations from memory or a data read, with no document request on an in-workspace switch                                                                                                                                                            |
| (b) prefetch plus view transitions | keep document navigations, prefetch the next document on hover or pointerdown, and cover the swap with cross-document view transitions                                                                                                                                                                                 |
| (c) hybrid                         | (a) wherever the data is already in memory: Content groups, newsletter, record open and close from the library, Operations views. Documents for cross-workspace switches, Life sections and deep-link fallbacks. Prefetch the editor chunk and record read on intent, and cache assets for every document that remains |

some fixes help all three and do not depend on the choice: the AppShell CSS bar, the `Updated` memo, the body cancel, the hidden-poll fixes and immutable asset caching.

(a) in-island pushState:

| favours                                                                                                                                                                         | disfavours                                                                                                                                                                                         |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Content prototype, 15 of 15: 0 documents, 0 requests, click to next frame 42 to 52 ms, one announcement, back stays in the document                                             | the leave guard must move into the router, and the dirty-editor flush path is untested                                                                                                             |
| removes from every switch: SSR CPU (8 to 21 ms, all of it in TTFB), 18 DO RPCs, ~50 full-200 asset requests, re-hydration, the repeated CLS and the idle-dwell document penalty | Life has no client read path. The owner reader denies timeline, preview and activity, and a new endpoint needs approval                                                                            |
| Content group rows and the Operations snapshot are already in props, and `LinkProvider` needs no Astryx patch                                                                   | an in-island record open loses the 404 and 503 document statuses, so HomeEditor needs real not-found and denied states                                                                             |
| the production estimate says only this path can reach 100 ms first change: document TTFB plus the CSS round trip is already 75 to 100 ms from EWR                               | three popstate owners exist today. Scroll on `#astryx-app-shell-main`, title and focus all become manual                                                                                           |
| one leave protocol replaces three pushState routers plus `admin:navigate`                                                                                                       | deep-linked records have no rows, and rows in memory go stale after edits                                                                                                                          |
|                                                                                                                                                                                 | the client render cost of switching to the 23-row overview is unmeasured (the prototype switched to writing, website and work). Timestamp cost grows super-linearly with rows until the memo lands |

(b) prefetch plus view transitions:

| favours                                                                                                                      | disfavours                                                                                                                                                                                                                           |
| ---------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| no router: HomeEditor's document listener, `beforeunload`, back, forward, scroll and deep links all keep working as they are | every switch still pays SSR CPU, the 18-RPC fan-out and hydration (105 to 130 ms from FCP on Content). Until they are fixed, it also pays the CLS and ~50 asset requests                                                             |
| the smallest code change, and no stale in-memory data                                                                        | admin documents are `private, no-store`, so reuse of a prefetched response is unverified. Astro prefetch goes through the HTTP cache and is likely useless here. Speculation-rules prerender runs page JS, including mount refreshes |
| intent prefetch can start the document before the click                                                                      | view transitions animate the swap but cannot paint before the new document renders, so first change stays tied to the document (estimated 110 to 165 ms signed in)                                                                   |
|                                                                                                                              | the harness fires hover, pointerdown and click within 1 ms, so it cannot credit any prefetch gain yet                                                                                                                                |
|                                                                                                                              | `apps/admin` has no view-transition code today. Reduced-motion CSS cancels animations, a transition on every switch conflicts with the house rule of one orchestrated motion, and browser support was not checked this phase         |
|                                                                                                                              | ClientRouter-style swaps re-hydrate both AdminLayout roots, which phase 1 already argued against                                                                                                                                     |

(c) hybrid:

| favours                                                                                                                                              | disfavours                                                                                                                   |
| ---------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------- |
| follows where the data already lives: Content and Operations switch from memory, and Life waits for an approved reader                               | two navigation models to build, test and measure, and the harness needs both first-change signals                            |
| pointerdown prefetch of the editor chunk and `record` read speeds up record opens either in the island or as documents                               | adding view transitions on the document path would create a third behaviour, so leave them out                               |
| asset caching and modulepreload make the remaining document switches cheaper, removing an estimated 190 to 280 ms script burst after the first visit | resolver boundaries must be explicit, so each URL is either claimed by an island or loaded as a document, never half of each |
| the build order ships the proven piece (Content groups) first and defers the untested ones (dirty-editor leave, Life)                                |                                                                                                                              |

## 10. decisions for Ani

| #   | decision                                  | recommended default                                                                                                                                                                                                                                                                            |
| --- | ----------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | admin asset caching                       | its own PR: exclude `/_astro/*` from `run_worker_first` and add `apps/admin/public/_headers` with `private, max-age=31536000, immutable`. www gets the `public` version in a separate PR. Send `Clear-Site-Data: "cache"` on in-app sign out, and verify with a signed-in browser after deploy |
| 2   | Life section navigation in this overhaul  | keep document loads and add no private JSON endpoint. Revisit once the owner reader lands; its method allowlist would need timeline, preview and activity                                                                                                                                      |
| 3   | client storage under the memory-only rule | in-island rows and records stay in memory only. Editorial recovery stays in localStorage as protection for unsaved work (in-app sign out already clears it). Also clear sessionStorage `admin:navigation:*` on sign out                                                                        |
| 4   | `Server-Timing`, now in admin production  | send it only on authenticated owner responses, never on 401, 403 or redirects. A signed-in production after-measurement can still read it                                                                                                                                                      |
| 5   | production after-measurement              | Ani runs one signed-in harness capture from his own browser session before the overhaul and one after, with no Access bypass and no staging host outside Access                                                                                                                                |

result: phase 2 answers every understand-phase question. The first proven build is the router module with Content group switching. The AppShell CSS bar, `Updated` memo, body cancel and hidden-poll fixes land with it, and asset caching goes in a separate PR once Ani approves.

report file: scratch/speed/phase2-report.md
